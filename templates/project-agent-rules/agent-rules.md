# 專案 agent 規則（core）

> 範例 core 檔。頂層檔的內容會被「內嵌」進根設定檔，請保持精簡、只放每次都要遵守的常駐規則。

## 專案概述

- 一句話說明這個專案是什麼、目標是什麼。
- 主要技術棧 / 框架。

## 工作守則

- 動手前先讀相關程式碼再改，不要憑空假設。
- 改動以最小、可回溯為原則。
- 跨專案：可讀其他專案作參考，但**不可修改其他專案**的任何檔案。

## 延伸資料

需要更深入的資訊時再去讀（生成時這段會由 agent 自動補成指向 `wiki/`、`reference/` 的索引）。
