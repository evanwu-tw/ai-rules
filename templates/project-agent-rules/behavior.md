# 本專案例外行為（core）

> 範例 core 檔。**只放本專案跟全域不同的地方**——通用語氣、動手原則、產出規範已在全域 `~/agent-rules`（會自動合併），不要在這裡重抄。
> 沒有例外的話，這個檔可以保持空白或直接刪掉。
> **放置原則**（什麼該放 core / agent-context / hook / memory / skill）見 `GENERATE.vendored.md` §0 決策表——強制性規則用 hook，不要只寫成這裡的 markdown。

<在此列出本專案的例外，例如：>

- （範例）本專案 commit 訊息一律用英文。
- （範例）禁止改動 `legacy/` 目錄。
